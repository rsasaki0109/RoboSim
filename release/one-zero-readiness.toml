schema_version = 4
# External conformance entries must retain their exact tested subjects; a
# report-only entry is intentionally not representable. Platform releases also
# require separate archive/install-report attestations and the extracted
# release report plus SHA256SUMS chain. Schema v4 separately audits external
# accelerator adapters without letting them satisfy the 1.0 external-system gate.
release_version = "0.1.0"
project_owner = "rsasaki0109"
minimum_stability_days = 183
minimum_external_projects = 2
minimum_compatibility_checks = 36
unplanned_breaking_changes = 0
blocker_registry = "release/blockers.toml"
required_platforms = ["linux_x86_64", "windows_x86_64"]
accelerator_adapter = []
compatibility_report = { path = "evidence/compatibility-report-v1.json", sha256 = "sha256:cecb0f35f2ea48cf9cdf420df054fda0b6a5a43dd7aa23d92e9bef89da910f4d" }

[candidate]
revision = "9e1ea8cb2dab8f468cf7bc51fac8bd381359acc4"
tree = "5d888b77e0d4785fa57c9edd10ce1292089fab5a"
since = "2026-08-20"

# These fields remain intentionally empty until maintainers make and publish
# a concrete compatibility-support commitment.
[support]
committed = false
maintainer = ""
support_period = ""
policy_url = ""
