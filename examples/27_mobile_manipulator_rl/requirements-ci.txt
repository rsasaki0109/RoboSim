# Pinned deps for xtask mobile_manipulator_rl_smokes (SB3 pulls torch; keep versions stable in CI).
# CPU-only torch avoids multi-GB CUDA wheels on Linux CI runners.
--extra-index-url https://download.pytorch.org/whl/cpu
gymnasium==1.0.0
numpy==2.1.3
stable-baselines3==2.6.0
torch==2.5.1
